default:
  suites:
    gloria:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@gloria'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'http://local.gloria.fi'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'

develop:
  suites:
    gloria:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@gloria'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'https://sanomalifestyle:kanapalat@dev-gloria.smf.aws.sanomacloud.net'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'

staging:
  suites:
    gloria:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@gloria'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'https://sanomalifestyle:kanapalat@staging-gloria.smf.aws.sanomacloud.net'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'

production:
  suites:
    gloria:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@gloria'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'https://www.gloria.fi'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'
