default:
  suites:
    vauva:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@vauva'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'http://local.vauva.fi'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'

develop:
  suites:
    vauva:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@vauva'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'https://sanomalifestyle:kanapalat@dev-vauva.smf.aws.sanomacloud.net'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'

staging:
  suites:
    vauva:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@vauva'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'https://sanomalifestyle:kanapalat@staging-vauva.smf.aws.sanomacloud.net'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'

production:
  suites:
    vauva:
      paths:
        - '%paths.base%/tests/features'
      filters:
        tags: '@vauva'
      contexts:
        - FeatureContext:
            parameters:
              base_url: 'https://www.vauva.fi'
        - DrupalContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\MessageContext
        - Drupal\DrupalExtension\Context\DrushContext
        - IntegratedExperts\BehatScreenshotExtension\Context\ScreenshotContext
      extensions:
        Drupal\DrupalExtension:
          drupal:
            # This assumes that we're running Behat from within
            drupal_root: '/vagrant/drupal/current'

